# Git Repository Template

This is a template git repository for evaluation testing.

## Structure

- `src/` - Source code directory
- `docs/` - Documentation
- `config/` - Configuration files

## Usage

This fixture is used to test git-related operations in evaluation trials.
